services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: pesquisa_preco
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
